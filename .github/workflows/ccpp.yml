name: C/C++ CI

on: [push]

jobs:
  server:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: make
      run: make
  client:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
